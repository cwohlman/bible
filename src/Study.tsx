import * as React from "react";
export default function Study({ searchTerm, results }) {
  return <div className="lg:w-1c my-5 lg:m-5 p-5 rounded-lg shadow-lg lg:shrink-0 max-h-full bg-white border border-gray-200">{searchTerm}</div>
}